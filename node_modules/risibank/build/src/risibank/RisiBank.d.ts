import { IRisiBank } from "./IRisiBank";
import { Sticker } from "./model";
export declare class RisiBank implements IRisiBank {
    static readonly PROTOCOL: string;
    static readonly HOST: string;
    static readonly SEARCH_URL: string;
    searchStickers(search: string): Promise<Sticker[]>;
}
